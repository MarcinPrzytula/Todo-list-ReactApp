export interface DefaultStateI {
  id: string;
  name: string;
  isImportant: boolean;
  isChecked: boolean;
  date: string;
  finishDate: string;
}

export interface DefaultStatePopupI {
  id: string;
  active: boolean;
}
